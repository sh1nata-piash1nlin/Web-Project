<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{article.title}}</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.6.2/css/bootstrap.min.css">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: Arial, sans-serif;
    }
    .article-header {
      margin-bottom: 30px;
      text-align: center;
    }
    .article-header img {
      max-width: 100%;
      height: auto;
      border-radius: 8px;
    }
    .article-content {
      margin-bottom: 40px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom sticky-top">
    <div class="container">
      <a class="navbar-brand" href="/">NNN</a>
    </div>
  </nav>

  <!-- Article Content -->
  <div class="container my-4">
    <div class="article-header">
      <h1 class="text-primary">{{article.title}}</h1>
      <img src="/public/images/{{article.featured_image}}" alt="{{article.title}}" class="img-fluid">
      <p><strong>Author:</strong> {{author}}</p>
      <p><strong>Category:</strong> {{category.name}}</p>
      <p><strong>Publish Date:</strong> {{article.publish_date}}</p>
      <p><strong>Tags:</strong> 
        {{#each tags}}
          <span class="badge badge-secondary">{{this.name}}</span>
        {{/each}}
      </p>
    </div>

    <div class="article-content">
      <p>{{article.content}}</p> <!-- Display the full content of the article -->
    </div>

    <div class="text-center">
      <a href="/editor" class="btn btn-primary">Back to Editor Dashboard</a>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-light text-center py-3">
    <p class="mb-0">&copy; 2024 NNN. All rights reserved.</p>
  </footer>

  <!-- Bootstrap JavaScript -->
  <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
