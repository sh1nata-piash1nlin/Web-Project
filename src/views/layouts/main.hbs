<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title> <!-- Dynamic Title -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <style>
        .navbar-brand img {
            height: 40px;
        }

        .main-nav .nav-link {
            color: #222;
            padding: 0.5rem 1rem;
            font-size: 14px;
            white-space: nowrap;
        }

        .sidebar {
            position: sticky;
            top: 20px;
        }

        .article-title {
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 1rem;
        }

        .article-meta {
            color: #666;
            font-size: 0.9rem;
        }

        .comment-count {
            color: #666;
            font-size: 0.8rem;
        }

        .related-news {
            border-left: 3px solid #e5e5e5;
            padding-left: 15px;
        }

        .related-news h3 {
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        /* Thêm style cho dropdown menu */
        .dropdown-menu {
            border-radius: 0;
            margin-top: 10;
            border-top: none;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 0%;
        }

        .dropdown-item {
            font-size: 14px;
            padding: 0.5rem 1rem;
        }

        .dropdown:hover .dropdown-menu {
            display: block;
        }

        .nav-item.dropdown:hover .nav-link {
            background-color: #f8f9fa;
        }

        .home-icon {
            font-size: 18px;
            transition: transform 0.3s, color 0.3s;
        }

        .home-icon:hover {
            color: #007bff;
        }

        .featured-article img {
            max-width: 200%;
            /* Đảm bảo hình không vượt quá chiều rộng khung chứa */
            height: auto;
            /* Giữ tỷ lệ hình ảnh */
            width: 750px;
            /* Đặt chiều rộng cụ thể (có thể thay đổi theo ý bạn) */
            border-radius: 5px;
            /* Tùy chọn: bo góc hình ảnh */
        }

        .col-lg-6 img {
            margin-top: 20px;
            /* Tạo khoảng cách phía trên ảnh */
            margin-bottom: 20px;
            /* Khoảng cách phía dưới ảnh */
            margin-left: 35px;
            /* Khoảng cách từ bên trái */
            display: block;
            /* Đảm bảo ảnh không bị dính margin của flexbox */
            max-width: 300px;
            /* Giới hạn chiều rộng ảnh */
            height: auto;
            /* Tự động điều chỉnh chiều cao */
            border-radius: 10px;
            /* Bo tròn góc ảnh */
        }

        .card {
            display: flex;
            flex-direction: column;
        }


        /* Chỉnh cỡ chữ cho tiêu đề trong cột thứ hai */
        /* Cột thứ nhất */
        .row .col-lg-6 .card-body {
            padding-left: 0;
            /* Loại bỏ khoảng cách bên trái */
            padding-right: 0;
            /* Loại bỏ khoảng cách bên phải */
        }

        .row .col-lg-6 img {
            width: 100%;
            /* Đảm bảo ảnh chiếm toàn bộ chiều rộng cột */
            height: auto;
            /* Điều chỉnh chiều cao ảnh tự động */
            border-radius: 10px;
            /* Bo tròn góc ảnh */
            margin-top: 20px;
            /* Khoảng cách phía trên */
            margin-bottom: 20px;
            /* Khoảng cách phía dưới */
        }

        /* Cột thứ hai */
        .row .col-lg-6 h4 {
            font-size: 1.1rem;
            line-height: 1.5;
            margin-bottom: 10px;
            word-wrap: break-word;
            /* Đảm bảo không bị tràn */
        }

        .row .col-lg-6 a {
            color: #343a40;
            font-weight: bold;
            text-decoration: none;
        }

        .row .col-lg-6 a:hover {
            color: #007bff;
            /* Màu khi hover */
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <div class="me-3">VnExpress</div>
            </a>

            <div class="d-flex align-items-center">
                <div class="me-3">Mới nhất</div>
                <div class="me-3">International</div>
                {{!-- <a class="btn btn-outline-dark btn-sm" href="/login">Login</a> --}}

            {{#if isAuthenticated}}
                <!-- Show the avatar when the user is logged in -->
                <div class="dropdown">
                    <img src="{{authUser.avatar}}"
                        onerror="this.onerror=null; this.src='/src/static/img/default.png';"
                        alt="User Avatar" 
                        class="rounded-circle" 
                        style="width: 40px; height: 40px; cursor: pointer;"
                        id="navbarDropdownAvatar" role="button" data-bs-toggle="dropdown" aria-expanded="false">

                    <!-- Dropdown menu for profile options -->
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownAvatar">
                        <li><a class="dropdown-item" href="/profile">Profile</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <form action="/logout" method="POST">
                                <button type="submit" class="dropdown-item text-danger">Logout</button>
                            </form>
                        </li>
                    </ul>
                </div>
            {{else}}
                <!-- Show the Login button if the user is not logged in -->
                <a class="btn btn-outline-dark btn-sm" href="/login">Login</a>

            {{/if}}
            </div>

        
            </div>
        </div>
    </nav>

    <!-- Secondary Navbar -->
    <nav class="navbar navbar-expand-lg bg-white border-bottom py-0">
        <div class="container">
            <div class="navbar-collapse">
                <ul class="navbar-nav main-nav d-flex justify-content-evenly w-100">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-house-door home-icon"></i>
                        </a>
                    </li>
                    {{#each categories}}
                    <li class="nav-item dropdown">
                        <!-- Hiển thị danh mục cha -->
                        <a class="nav-link dropdown-toggle" href="{{this.link}}" id="navbarDropdown{{this.id}}"
                            role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {{this.name}}
                        </a>
                        {{#if this.subcategories.length}}
                        <!-- Chỉ hiển thị dropdown khi có danh mục con -->
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown{{this.id}}">
                            {{#each this.subcategories}}
                            <li><a class="dropdown-item" href="{{this.link}}">{{this.name}}</a></li>
                            {{/each}}
                        </ul>
                        {{/if}}
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container mt-4">
        <div class="row">
            <!-- Main Content Column -->
            <div class="col-lg-8">
                {{{body}}}
            </div>

            <!-- Sidebar -->
            <div class="col-lg-4">
                <div class="sidebar">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h5 class="mb-0">Tin nổi bật</h5>
                        </div>
                        <div class="row mb-4">
                            {{#each featuredArticles}}
                            <div class="col-lg-6 d-flex align-items-center justify-content-center">
                                <!-- Phần ảnh -->
                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded"
                                    style="width: 100%; max-width: 300px; height: auto; border-radius: 10px; margin-top: 20px; margin-bottom: 20px;">
                            </div>
                            <div class="col-lg-6 d-flex align-items-center">
                                <!-- Phần nội dung -->
                                <div class="card-body">
                                    <div class="related-news mb-3">
                                        <h3>
                                            <a href="{{this.link}}"
                                                class="text-dark text-decoration-none">{{this.title}}</a>
                                        </h3>
                                        <div class="comment-count">
                                            <i class="bi bi-chat-dots"></i> {{this.comments}}
                                        </div>
                                    </div>
                                </div>
                            </div>
                            {{/each}}
                        </div>


                    </div>

                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">Tin mới nhất</h5>
                        </div>
                        <div class="card-body">
                            {{#each featuredArticles}}
                            <div class="related-news mb-3">
                                <h3><a href="{{this.link}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                </h3>
                            </div>
                            {{/each}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <section class="bg-white py-5">
        <div class="container">
            <div class="row">
                <!-- Left Section: 60% -->
                <div class="col-lg-7 mb-4">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-header bg-dark text-white d-flex justify-content-between">
                            <h3 class="mb-0">Tin vắn</h3>
                            <a href="category-01.html" class="btn btn-outline-light btn-sm">
                                View All
                                <i class="fas fa-caret-right ml-2"></i>
                            </a>
                        </div>
                        <div class="card-body">
                            <ul class="nav nav-tabs" id="categoryTabs" role="tablist">
                                <li class="nav-item">
                                    <a class="nav-link active" id="all-tab" data-bs-toggle="tab" href="#tab-all-content" role="tab">
                                        Tất cả
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" id="health-tab" data-bs-toggle="tab" href="#tab-health-content" role="tab">
                                        Sức khỏe
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" id="life-tab" data-bs-toggle="tab" href="#tab-life-content" role="tab">
                                        Đời sống
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" id="tech-tab" data-bs-toggle="tab" href="#tab-tech-content" role="tab">
                                        Công nghệ
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" id="car-tab" data-bs-toggle="tab" href="#tab-car-content" role="tab">
                                        Xe
                                    </a>
                                </li>
                            </ul>

                            <div class="tab-content mt-3">
                                <!-- Tab Tất cả -->
                                <div class="tab-pane fade show active" id="tab-all-content" role="tabpanel">
                                    <div class="row">
                                        {{#each featuredArticles}}
                                        <div class="col-lg-6">
                                            {{#if @first}}
                                            <div class="main-article mb-4">
                                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded" alt="{{this.title}}">
                                                <h3 class="mt-3">
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h3>
                                                <p class="text-muted">{{this.abstract}}</p>
                                            </div>
                                            {{else}}
                                            <div class="related-article mb-3">
                                                <h4>
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h4>
                                            </div>
                                            {{/if}}
                                        </div>
                                        {{/each}}
                                    </div>
                                </div>

                                <!-- Tab Sức khỏe -->
                                <div class="tab-pane fade" id="tab-health-content" role="tabpanel">
                                    <div class="row">
                                        {{#each featuredArticles}}
                                        <div class="col-lg-6">
                                            {{#if @first}}
                                            <div class="main-article mb-4">
                                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded" alt="{{this.title}}">
                                                <h3 class="mt-3">
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h3>
                                                <p class="text-muted">{{this.abstract}}</p>
                                            </div>
                                            {{else}}
                                            <div class="related-article mb-3">
                                                <h4>
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h4>
                                            </div>
                                            {{/if}}
                                        </div>
                                        {{/each}}
                                    </div>
                                </div>

                                <!-- Tab Đời sống -->
                                <div class="tab-pane fade" id="tab-life-content" role="tabpanel">
                                    <div class="row">
                                        {{#each featuredArticles}}
                                        <div class="col-lg-6">
                                            {{#if @first}}
                                            <div class="main-article mb-4">
                                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded" alt="{{this.title}}">
                                                <h3 class="mt-3">
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h3>
                                                <p class="text-muted">{{this.abstract}}</p>
                                            </div>
                                            {{else}}
                                            <div class="related-article mb-3">
                                                <h4>
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h4>
                                            </div>
                                            {{/if}}
                                        </div>
                                        {{/each}}
                                    </div>
                                </div>

                                <!-- Tab Công nghệ -->
                                <div class="tab-pane fade" id="tab-tech-content" role="tabpanel">
                                    <div class="row">
                                        {{#each featuredArticles}}
                                        <div class="col-lg-6">
                                            {{#if @first}}
                                            <div class="main-article mb-4">
                                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded" alt="{{this.title}}">
                                                <h3 class="mt-3">
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h3>
                                                <p class="text-muted">{{this.abstract}}</p>
                                            </div>
                                            {{else}}
                                            <div class="related-article mb-3">
                                                <h4>
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h4>
                                            </div>
                                            {{/if}}
                                        </div>
                                        {{/each}}
                                    </div>
                                </div>

                                <!-- Tab Xe -->
                                <div class="tab-pane fade" id="tab-car-content" role="tabpanel">
                                    <div class="row">
                                        {{#each featuredArticles}}
                                        <div class="col-lg-6">
                                            {{#if @first}}
                                            <div class="main-article mb-4">
                                                <img src="/public/images/{{this.featured_image}}" class="img-fluid rounded" alt="{{this.title}}">
                                                <h3 class="mt-3">
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h3>
                                                <p class="text-muted">{{this.abstract}}</p>
                                            </div>
                                            {{else}}
                                            <div class="related-article mb-3">
                                                <h4>
                                                    <a href="/article/{{this.id}}" class="text-dark text-decoration-none">{{this.title}}</a>
                                                </h4>
                                            </div>
                                            {{/if}}
                                        </div>
                                        {{/each}}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Right Section: 40% -->
                <div class="col-lg-5">
                    <div class="card border-0 shadow-sm h-100">
                        <div class="card-body bg-white text-black">
                            <h5 class="card-title">Bài viết được thảo luận nhiều nhất</h5>
                            <!-- Debug info -->
                            {{#if debug.hasMostCommented}}
                                <p class="text-muted">Có dữ liệu</p>
                            {{else}}
                                <p class="text-muted">Không có dữ liệu</p>
                            {{/if}}
                            
                            <ul class="list-unstyled">
                                {{#each mostCommentedArticles}}
                                <li class="mb-3">
                                    <a href="/article/{{this.id}}" class="text-dark">
                                        <i class="fas fa-chevron-right mr-2"></i>
                                        {{this.title}}
                                        <small class="text-muted">({{this.comment_count}})</small>
                                    </a>
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    

    <!-- Footer -->
    <footer class="bg-dark text-light mt-5">
        <div class="container py-4">
            <div class="row">
                <div class="col-md-4">
                    <h5>Về chúng tôi</h5>
                    <!-- Footer content -->
                </div>
                <div class="col-md-4">
                    <h5>Chuyên mục</h5>
                    <!-- Categories -->
                </div>
                <div class="col-md-4">
                    <h5>Liên hệ</h5>
                    <!-- Contact info -->
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>