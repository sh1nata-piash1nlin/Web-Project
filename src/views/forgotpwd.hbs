<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Forgot Password</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      body {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: linear-gradient(135deg, #e8d6d6, #c1b0b0);
      }
      .forgot-container {
        padding: 30px;
        background: white;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body>
    <div class="forgot-container">
      <h3 class="text-center">Reset your password</h3>
      <p class="text-center">Enter your email to proceed</p>

      {{#if showErrors}}
        <div class="alert alert-danger">Please enter a valid email address.</div>
      {{/if}}

      <form method="post" action="/login/forgotpwd">
        <div class="mb-3">
          <input type="email" name="email" class="form-control" placeholder="Email address" required />
        </div>
        <button type="submit" class="btn btn-dark w-100">Next</button>
      </form>
    </div>
  </body>
</html>